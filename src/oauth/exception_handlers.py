"""OAuth exception handlers — filled in Task 3.

Registered on the global ``NinjaExtraAPI`` from :meth:`oauth.apps.OauthConfig.ready`.
"""


def register() -> None:
    """Install oauth exception handlers on the global Ninja API."""
    return None
